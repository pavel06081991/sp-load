{
  "name": "sp-load",
  "version": "1.0.3",
  "description": "Modules loader. No lots of require calls on the top of files. On demand modules loading. Local modules.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "pavel06081991/sp-load",
  "keywords": [
    "loading",
    "load",
    "load modules on demand",
    "load module on demand",
    "load on demand",
    "modules on demand",
    "module on demand",
    "on demand",
    "lazy",
    "lazy load",
    "lazy modules",
    "lazy module",
    "require",
    "require on demand",
    "require modules on demand",
    "require module on demand",
    "local modules",
    "local"
  ],
  "author": "pavel06081991",
  "license": "ISC",
  "dependencies": {
    "findup-sync": "^0.3.0",
    "lodash": "^3.10.1",
    "is-there": "^4.3.0",
    "string-to-regexp": "^0.1.0"
  }
}
